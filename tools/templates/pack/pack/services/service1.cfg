define service{
   host_name	  	        {{ name }}
   service_description      service1
   use            	        {{ name }}-service
   name                     {{ name }}-service1
   register       	        0
   check_command            COMMAND_NAME
}
