define service{
   host_name	  	        {{ name }}
   service_description      service1.cfg
   use            	        {{ name }}-service
   name                     {{ name }}-service
   register       	        0
   check_command            COMMAND_NAME
}
