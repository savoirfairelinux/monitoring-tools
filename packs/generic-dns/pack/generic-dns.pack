{
"name":"generic-dns",

"description":"DNS active checks with DNS requests",

"path":"services/",

"macros":{
         "_DNSHOSTNAME":        {"type": "string",
                                 "description": "Hostname to resolve",
                                },
         "_DNSEXPECTEDRESULT":   {"type": "string",
                                 "description": "Address expected returned by the DNS server",
                                },
        }
}