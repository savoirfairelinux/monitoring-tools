define service {
    service_description     Mongo Updates per Second
    use                     generic-mongodb-service
    register                0
    host_name               generic-mongodb
    check_command           check_mongodb_queries_per_second
}