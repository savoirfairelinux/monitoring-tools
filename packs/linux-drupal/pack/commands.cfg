# -----------------------------------------------------------------
#
#      Linux-drupal Commands
#
# -----------------------------------------------------------------

define command {
	command_name                          check_drupal_cache
	command_line                          $PLUGINSDIR$/check_drupal_cache -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_codebase
	command_line                          $PLUGINSDIR$/check_drupal_codebase -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_cron
	command_line                          $PLUGINSDIR$/check_drupal_cron -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_database
	command_line                          $PLUGINSDIR$/check_drupal_database -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_extensions
	command_line                          $PLUGINSDIR$/check_drupal_extensions -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_logging
	command_line                          $PLUGINSDIR$/check_drupal_logging -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_security
	command_line                          $PLUGINSDIR$/check_drupal_security -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_status
	command_line                          $PLUGINSDIR$/check_drupal_status -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}

define command {
	command_name                          check_drupal_views
	command_line                          $PLUGINSDIR$/check_drupal_views -w $_DRUPAL_WARN$ -c $_DRUPAL_CRIT$ $_DRUPAL_OPTION$
}
